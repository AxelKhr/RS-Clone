import './index.scss';

import type ITheme from '@/types/theme';

export default {
    id: 'light',
    name: 'Light',
    color: '#FFFFFF',
    fontColor: '#353539',
    class: 'theme--light',
} as ITheme;
