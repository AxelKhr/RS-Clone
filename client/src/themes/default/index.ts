import './index.scss';

import type ITheme from '@/types/theme';

export default {
    id: 'default',
    name: 'Default',
    color: '#FFFFFF',
    fontColor: '#353539',
    class: 'theme--default',
} as ITheme;
